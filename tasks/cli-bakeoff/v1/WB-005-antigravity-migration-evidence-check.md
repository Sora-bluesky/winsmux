# WB-005: Antigravity CLI Migration Evidence Check

You are one worker in a winsmux desktop comparison run.

Assess what evidence winsmux needs before treating Antigravity CLI as a reliable
Gemini CLI successor in worker panes. Do not edit files.

Return exactly this structure:

1. `BAKEOFF_ROUND_A_BEGIN`
2. Required local evidence.
3. Migration risks.
4. A test plan for `agy` non-interactive execution.
5. `BAKEOFF_ROUND_A_END`

Quality bar:

- Require local `agy` version evidence.
- Require selected model evidence.
- Treat empty `--print` stdout as blocked, not successful.
